<div>
  <%= cl_image_tag @painting.photo.key, height: 300, width: 400, crop: :fill %>
  <%= simple_form_for(@painting) do |f| %>
  <%= f.input :photo, as: :file %>
  <%= f.button :submit %>
<% end %>
</div>
